import { Box, Typography } from "@mui/material";

export const Media = () => (
  <Box
    sx={{
      display: "flex",
      height: "100vh",
      justifyContent: "center",
      alignItems: "center",
    }}
  >
    <Typography variant="h4">Media !</Typography>
  </Box>
);
